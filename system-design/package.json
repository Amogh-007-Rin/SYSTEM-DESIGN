{
  "name": "system-design-mastery",
  "version": "1.0.0",
  "private": true,
  "description": "Runtime dependencies for the TypeScript code examples and exercises in this repository. The content itself is Markdown; this package.json exists only so `npx ts-node <file>.ts` works out of the box for every example.",
  "license": "MIT",
  "scripts": {
    "check-structure": "bash scripts/check-structure.sh",
    "validate-links": "bash scripts/validate-links.sh"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/jsonwebtoken": "^9.0.6",
    "@types/node": "^25.9.0",
    "@types/ws": "^8.5.12",
    "express": "^4.21.1",
    "jsonwebtoken": "^9.0.2",
    "prom-client": "^15.1.3",
    "ts-node": "^10.9.2",
    "typescript": "^5.6.3",
    "ws": "^8.18.0"
  }
}
